{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "globalEnv": [
    "NODE_ENV",
    "DATABASE_URL",
    "REDIS_URL",
    "SUPABASE_URL",
    "SUPABASE_ANON_KEY",
    "SUPABASE_SERVICE_ROLE_KEY",
    "JWT_SECRET"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "@clickbit/api#db:generate"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["NODE_ENV", "DATABASE_URL", "SUPABASE_URL", "SUPABASE_SERVICE_ROLE_KEY", "JWT_SECRET"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["@clickbit/api#db:generate"]
    },
    "test": {
      "dependsOn": ["^build", "@clickbit/api#db:generate"]
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build", "@clickbit/api#db:generate"]
    },
    "db:generate": {
      "cache": false,
      "outputs": ["node_modules/.prisma/client/**"]
    },
    "db:pull": {
      "cache": false
    }
  }
}
