{
  "name": "@clickbit/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and DTOs for clickbit-staged",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "class-transformer": "*",
    "class-validator": "*"
  }
}
